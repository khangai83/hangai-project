import QueueClient from '../../../components/QueueClient';

export const metadata = { title: 'Facebook агент — ZAR.mn' };
export const dynamic = 'force-dynamic';

export default function AdminQueuePage() {
  return <QueueClient />;
}
